<!-- app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".NotaMuApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotaMu">

        <activity
            android:name=".MainActivity"
            android:exported="true">   <!-- ✅ WAJIB kalau ada intent-filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Komponen lain tanpa intent-filter tidak perlu exported -->
        <!-- Jika kamu punya Activity/Service/Receiver lain dengan intent-filter,
             set juga android:exported="true/false" sesuai kebutuhan. -->
    </application>
</manifest>
